# Optional: bake an Anthropic API key into the build for AI marking of
# writing/speaking. Copy this file to `.env` (git-ignored) and fill it in.
# You can also paste a key at runtime in the app's Settings screen instead —
# that one is stored only in your browser's localStorage.
VITE_ANTHROPIC_API_KEY=
